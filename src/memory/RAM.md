Every request should have 64*n size to faster speed even if the fragmentation is big.


Page Allocator.
- UUID
- EMAIL / identifyier (sha256)
- PW (sha256) fix

With UUID there can be constructor built
-> Name, etc.